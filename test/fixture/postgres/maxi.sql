CREATE SCHEMA maxi;

CREATE TABLE maxi.users1 (
  email text NOT NULL,
  id bigint NOT NULL
);

CREATE TABLE maxi.users2 (
  email text NOT NULL,
  id bigint NOT NULL
);

CREATE TABLE maxi.users3 (
  email text NOT NULL,
  id bigint NOT NULL
);
